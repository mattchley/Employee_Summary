const Employee = require("./employee");

class Manager extends Employee {
    constructor(phone) {
     
      super(name, employeeType, email);
      // Objects in constructor
      this.phone = phone;
    }
  };